from django.urls import path, include
from . import views

clustering = 'clustering'

urlpatterns = [
    # path('test/', views.Test.as_view(), name='test'),
    # path('model/<str:model_name>', views.BuildModel.as_view(), name='build_model'),
    # path('predict/<str:model_name>', views.PredictionEngine.as_view(), name='prediction_engine'),
    # path('docs/<str:dataset_name>', views.DocumentRetreival.as_view(), name='document_reteival'),
]
